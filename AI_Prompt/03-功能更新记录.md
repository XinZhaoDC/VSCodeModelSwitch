# VSCodeModelSwitch 功能更新记录

本文件记录每次仓库更新的用户可见功能、配置影响和验证结果。它不是
Git 提交的替代品；每条记录应能追溯到对应 commit、VSIX 和测试命令。

## 记录规则

每次功能更新至少记录：

- 日期和版本号。
- 用户可见功能变化。
- 修改的主要文件或模块。
- 配置文件、SecretStorage、环境变量是否变化。
- 是否需要迁移、重新输入 key 或重新安装 VSIX。
- 执行过的检查、编译、打包和运行验证。
- 已知限制、未验证范围和回滚版本。

不要在本文件或 Git 提交中写入真实 API key、OAuth token、Cookie、完整
认证配置或带密钥的日志。

## 版本记录模板

```markdown
## vX.Y.Z - YYYY-MM-DD

### 功能

-

### 行为变化

-

### 安全与配置

-

### 修改范围

-

### 验证

- `npm run check`
- `npm run compile`
- `npm run package`
-

### 限制与回滚

- 未验证：
- 回滚 VSIX：
```

## Unreleased - 2026-09-05

### 网络与兼容性诊断

- 将基础网络可达性、Models API、模型协议兼容性和 Usage 查询拆分为独立结果。
- `/v1/models` 或 `/models` 不支持、返回 HTTP 错误时，不再直接判定整个 Provider 网络断开。
- Models API 均失败时，使用 Provider 域名根地址执行基础可达性回退检查。
- 网络结果记录最终端点、HTTP 状态、延迟、响应大小和 DNS 地址。
- 识别远程域名解析到容器回环地址的错误，并将 `198.18.0.0/15` 标记为 VPN Fake-IP。
- Claude 请求优先使用与 `ANTHROPIC_AUTH_TOKEN` 一致的 Bearer 认证；认证失败时兼容回退到 `x-api-key`。
- `Verify Model` 区分网络、认证、端点不支持和请求被拒绝，并参与 Active Provider 的全局状态判断。
- Usage 新增 `Auto Detect`，在同一 Provider 域名下依次尝试常见余额、用户信息、Key 状态和 credit-grants 端点，并匹配多种已知 JSON 结构。
- Auto Detect 扩展 Kimi、StepFun、OpenRouter Credits、Novita、积分余额、账户报表和 New API 数据端点，总查询时间限制为每个 Provider 20 秒。
- Auto Detect 可选使用独立的 Management Access Token 与 User ID 查询 New API/AgentRouter 管理接口；未提供时不会误用模型 key 请求这些管理接口。
- Usage 解析失败只记录 JSON 字段结构，不记录字段值或原始响应正文。
- 新增不记录密钥和响应正文的 `VSCodeModelSwitch` Output 通道，记录网络、Usage、兼容性和模型拉取诊断。

### 状态与执行行为

- 网络及兼容性结果按 Provider ID 保存，避免一个 Provider 的旧错误污染同工具的其他 Provider。
- 仅 DNS、传输层或真实模型验证的网络失败显示 `Network error`；认证和协议失败显示 `Degraded`。
- 修改模型或完整配置、删除 Provider 时清除对应的旧诊断结果并重新匹配实际配置。
- 工具级 `Network & Usage` 最多并发检查两个 Provider，取消操作会同步终止网络和 Usage 请求。
- 单独的 Usage 查询也支持取消。
- Add Provider 的模型控件支持直接输入 Model ID；Models API 不可用时不再阻塞模式 A 添加。
- Fetch Models 支持取消，失败提示和 Output 日志包含候选端点及 DNS 诊断。
- Key fields 模式只要求 Tool、Name、Base URL 和 API Key；Model ID 可留空，拉取成功时自动填入首个结果，失败时显示警告并保留手动输入。
- Provider 卡片与全局 Active 状态中的空 Model ID 统一显示为 `NULL`，且空模型时禁用 `Verify Model`。
- Full Configuration 不再被隐藏的模式 A 必填字段拦截；允许空 model 保存并显示警告，`effortLevel` 可以为空或省略。
- `Set active after saving` 默认关闭，命令面板添加流程也不再自动激活。
- 全局状态区域显示当前扩展日志目录及 Claude/Codex 备份目录大小。
- Claude/Codex 折叠按钮隐藏整个 Tool 面板；Enabled 开关继续作为操作锁。
- Configuration 区域新增带确认的 `Delete Log`，用于删除当前扩展日志目录中的普通日志文件并重建空日志通道。

### 安全与配置

- 未新增代理配置，TUN 模式继续使用容器现有系统路由。
- 未改变 Provider 持久化结构、SecretStorage 引用和实际 Claude/Codex 配置格式。
- 未输出或迁移任何 API key。
- Auto Detect 只访问由当前 Provider Base URL 派生出的同源端点；普通端点使用 Provider API key，管理端点只在用户另外提供相应管理凭据后请求。

### 修改范围

- `src/types.ts`
- `src/models/modelFetcher.ts`
- `src/models/compatibilityCheck.ts`
- `src/models/usageQuery.ts`
- `src/app.ts`
- `src/ui/sidebarView.ts`
- `scripts/mock-model-server.js`
- `README.md`
- `README.zh-CN.md`
- `docs/validation.md`
- `docs/product-logic.md`
- `package.json`

### 文档与测试支持

- 重写中英文 README 的安装、安全使用、Provider 管理、网络/Usage、日志备份、构建和 sandbox 回归说明。
- 验证指南移除已删除的全局 Apply 与独立 Set Key 流程，并避免在终端打印密钥值。
- 产品逻辑同步为可选 Model ID、完整配置替换、Base URL + key 匹配和 Config 内密钥维护。
- Mock 服务增加 Claude `/v1/messages` 与 Codex `/v1/responses`，支持离线 `Verify Model` 回归。
- 修正 GitHub 仓库、Issues 和 Homepage 元数据为 `XinZhaoDC/VSCodeModelSwitch`。
- Git 与 VSIX 显式排除插件日志、`vscodemodelswitchbak/`、Claude `settings.json` 和 Codex `config.toml`，降低自定义 sandbox 路径下误提交密钥的风险。

### 验证

- `git diff --check`：通过。
- `npm run check`：待手动执行。
- `npm run compile`：待手动执行。
- `npm run package`：待手动执行。
- VSIX 安装后 GUI 与真实 Provider 回归：待执行。

### 限制与回滚

- 网络与 Usage 结果只保存在当前扩展进程内，重载窗口后需要重新检查。
- `Verify Model` 会发送最小模型请求，可能产生极少量 API 用量。
- 部分中转站不允许使用模型 API key 查询账户余额；自动探测全部失败时仍需要站点提供专用 Usage API、Access Token 或 User ID。
- 显式 HTTP/Mixed Proxy 支持尚未加入；当前优先使用系统直连或 VPN TUN。

## v0.1.0 - 基线版本

### 功能

- 管理 Claude Code 和 Codex Provider。
- 请求并选择 Provider 模型列表。
- 使用 VS Code SecretStorage 保存 key。
- 在状态栏显示当前 Provider 和模型。
- 为新建 VS Code 终端注入 CLI 环境变量。
- 可选同步 CLI 配置文件。
- 支持公共配置导入和导出。
- 提供 sandbox 配置目标。

### 私有版本身份

- 扩展包名：`vscodemodelswitch`。
- 显示名称：`VSCodeModelSwitch`。
- 扩展 ID：`xinzhaodc.vscodemodelswitch`。
- 私有源码仓库：`XinZhaoDC/VSCodeModelSwitch`。
- 原始源码对照目录：`original/`。

### 验证

- Node.js 22.14.0 prefix 已安装。
- `npm ci` 已通过。
- `npm run check` 已通过。
- `npm run compile` 已通过。
- `npm run package` 已通过。
- VSIX 已生成：`vscodemodelswitch-0.1.0.vsix`。
- Marketplace 发布：未执行。
- VSIX 安装后的完整 sandbox 运行回归：待记录。

### 已知限制

- 当前额度查询、网络测速和完整配置文本导入尚未实现。
- Provider 额度接口需要按服务分别适配，不能假设所有服务都有统一接口。
- 现有版本仍有配置写入安全问题，后续版本应先修复再扩展功能。
